import * as React from "react";
import supplementarySummariesService from "../../services/SupplementarySummaries/supplementarySummariesService";




//declare var abp: any;

  const BackgroundJobDashboard: React.SFC = () => {
    // const [BackgroundtableData, setBackgroundTableData] = React.useState<any[]>([]);
    // const [procureBackgroundtableData, setprocureBackgroundTableData] = React.useState<any[]>([]);
    // const [cbfcBackgroundtableData, setcbfcBackgroundTableData] = React.useState<any[]>([]);
    // const [grnBackgroundtableData, setgrnBackgroundTableData] = React.useState<any[]>([]);
    
  console.log(supplementarySummariesService);

  return (
    <div>BackgrounJob</div>
      );
};

export default BackgroundJobDashboard;
